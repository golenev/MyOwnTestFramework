package elements;

import org.openqa.selenium.By;
import org.openqa.selenium.WebDriver;

public class TextBox extends AbstractElement{
    public TextBox(WebDriver webDriver, By by) {
        super(webDriver, by);
    }
}
